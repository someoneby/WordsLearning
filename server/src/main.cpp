#include <iostream>
#include <thread>

#include "ConnectionHandler/Server.hh"
//#include "DatabaseHandlers/DatabaseHandler.hh"
#include "RequestHandler/RequestHandler.hh"

int main(int argc, char* argv[])
{
	// std::thread th([]() {
	// 	while (true) {
	// 		std::cout << "main\n";
	// 		std::this_thread::sleep_for(std::chrono::seconds(1));
	// 	}
	// 	});
	// th.detach();

	//Server server;
	//server.addRequestHandler(std::make_shared<RequestHandler>());
	//server.start();
}